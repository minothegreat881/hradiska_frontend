# Strapi API base URL (no trailing slash)
VITE_STRAPI_URL=http://localhost:1337

# Mapbox public access token — free at https://account.mapbox.com/
# Mapbox dev demo token works for testing but produces "rate limited" in production.
VITE_MAPBOX_TOKEN=pk.your-token-here
